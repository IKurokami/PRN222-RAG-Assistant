@model PRN222.RagAssistant.Models.Admin.AdminUserCreateViewModel
@{
    ViewData["Title"] = "Thêm Người Dùng Mới - Admin";
}

<div class="container py-4">
    <!-- Breadcrumb -->
    <div class="academic-breadcrumb mb-3">
        <a asp-page="/Index"><i class="bi bi-house-door"></i> Trang chủ</a>
        <span class="academic-breadcrumb-separator">/</span>
        <a asp-controller="AdminUsers" asp-action="Index">Quản trị người dùng</a>
        <span class="academic-breadcrumb-separator">/</span>
        <span class="fw-semibold">Tạo tài khoản mới</span>
    </div>

    <div class="row justify-content-center">
        <div class="col-lg-7 col-xl-6">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <div>
                    <h1 class="fw-bold fs-3 text-dark mb-1">
                        <i class="bi bi-person-plus-fill text-primary me-2"></i>Thêm Người Dùng Mới
                    </h1>
                    <p class="text-muted small mb-0">Khởi tạo tài khoản và gán vai trò tương ứng trong hệ thống.</p>
                </div>
                <a asp-controller="AdminUsers" asp-action="Index" class="btn btn-sm btn-outline-secondary btn-press">
                    <i class="bi bi-arrow-left"></i> Quay lại
                </a>
            </div>

            <div class="academic-card card-lift shadow-sm bg-white border rounded-4 p-4">
                <form method="post" asp-controller="AdminUsers" asp-action="Create">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger small mb-4" role="alert" style="border-radius: var(--radius-md);"></div>

                    <div class="mb-4">
                        <label asp-for="DisplayName" class="form-label fw-bold small text-dark">
                            Họ và tên hiển thị <span class="text-danger">*</span>
                        </label>
                        <input asp-for="DisplayName" class="form-control input-focus-glow" autocomplete="name" placeholder="Ví dụ: Nguyễn Văn A" style="border-radius: var(--radius-md); padding: 0.65rem 0.85rem;" />
                        <span asp-validation-for="DisplayName" class="text-danger small mt-1 d-block"></span>
                    </div>

                    <div class="mb-4">
                        <label asp-for="Email" class="form-label fw-bold small text-dark">
                            Địa chỉ Email <span class="text-danger">*</span>
                        </label>
                        <input asp-for="Email" class="form-control input-focus-glow" autocomplete="email" placeholder="Ví dụ: user@fpt.edu.vn" style="border-radius: var(--radius-md); padding: 0.65rem 0.85rem;" />
                        <span asp-validation-for="Email" class="text-danger small mt-1 d-block"></span>
                    </div>

                    <div class="mb-4">
                        <label asp-for="Role" class="form-label fw-bold small text-dark">
                            Vai trò hệ thống (Role) <span class="text-danger">*</span>
                        </label>
                        <select asp-for="Role" class="form-select input-focus-glow" style="border-radius: var(--radius-md); padding: 0.65rem 0.85rem;">
                            <option value="@AppRoles.Student">Sinh viên (Student) — Tra cứu &amp; Hỏi đáp RAG</option>
                            <option value="@AppRoles.SubjectLeader">Trưởng môn (SubjectLeader) — Quản lý môn học được giao</option>
                            <option value="@AppRoles.Admin">Quản trị viên (Admin) — Toàn quyền hệ thống</option>
                        </select>
                        <span asp-validation-for="Role" class="text-danger small mt-1 d-block"></span>
                    </div>

                    <div class="row g-3 mb-4">
                        <div class="col-md-6">
                            <label asp-for="Password" class="form-label fw-bold small text-dark">
                                Mật khẩu <span class="text-danger">*</span>
                            </label>
                            <input asp-for="Password" class="form-control input-focus-glow" type="password" autocomplete="new-password" placeholder="••••••••" style="border-radius: var(--radius-md); padding: 0.65rem 0.85rem;" />
                            <span asp-validation-for="Password" class="text-danger small mt-1 d-block"></span>
                        </div>
                        <div class="col-md-6">
                            <label asp-for="ConfirmPassword" class="form-label fw-bold small text-dark">
                                Xác nhận mật khẩu <span class="text-danger">*</span>
                            </label>
                            <input asp-for="ConfirmPassword" class="form-control input-focus-glow" type="password" autocomplete="new-password" placeholder="••••••••" style="border-radius: var(--radius-md); padding: 0.65rem 0.85rem;" />
                            <span asp-validation-for="ConfirmPassword" class="text-danger small mt-1 d-block"></span>
                        </div>
                    </div>

                    <div class="d-flex justify-content-end gap-2 mt-4">
                        <a asp-controller="AdminUsers" asp-action="Index" class="btn btn-outline-secondary btn-press" style="border-radius: var(--radius-md);">Hủy bỏ</a>
                        <button type="submit" class="btn btn-primary btn-press px-4" style="background-color: var(--color-primary-600); border-color: var(--color-primary-600); border-radius: var(--radius-md); font-weight: 600;">
                            <i class="bi bi-check2-circle me-1"></i> Khởi tạo tài khoản
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
